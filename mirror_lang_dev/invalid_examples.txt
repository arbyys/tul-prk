// Example 1: Missing closing keyword
(SET $counter = 10 TS)  // Missing 'E' in 'TES)'

// Example 2: Incorrect variable naming (uppercase not allowed)
(SET $Counter = 5 TES)  // Variables must be lowercase

// Example 3: Missing $ in variable name
(SET counter = 15 TES)  // Variables must start with $

// Example 4: Incorrect keyword format
SET $x = 10 TES)  // Missing opening parenthesis
(SET $y = 20 TES  // Missing closing parenthesis

// Example 5: Incorrect keyword reversal
(SET $z = 30 SET)  // Should be 'TES)' not 'SET)'

// Example 6: Mismatched nested structures
(IF $a > $b
    (LOOPIF $i < 10
        (LOG "Inside loop" GOL)
    FI)  // Should be 'FIPOOL)' not 'FI)'
FIPOOL)  // Should be 'FI)' not 'FIPOOL)'

// Example 7: Invalid operators
(IF $x === $y  // Triple equals not supported
    (LOG "Equal" GOL)
FI)

// Example 8: Missing condition in IF
(IF
    (LOG "No condition" GOL)
FI)

// Example 9: Invalid comment format
/// This is not a valid comment format

// Example 10: Incorrect multi-line comment format
(IGNORE
    This is a multi-line comment
IGNORE)  // Should be 'ERONGI)' not 'IGNORE)'
